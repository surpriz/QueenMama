# =============================================================================
# Queen Mama - Port Configuration
# =============================================================================
# Copy this file to .env and customize the ports to avoid conflicts
# These environment variables are used by docker-compose.yml
# =============================================================================

# Frontend (Next.js)
QM_WEB_PORT=3002

# Backend API (NestJS)
QM_API_PORT=3003

# PostgreSQL Database
QM_POSTGRES_PORT=5432

# Redis Cache
QM_REDIS_PORT=6380

# MinIO S3-compatible Storage
QM_MINIO_API_PORT=9000
QM_MINIO_CONSOLE_PORT=9001

# =============================================================================
# Example: Alternative ports to avoid conflicts
# =============================================================================
# QM_WEB_PORT=3100
# QM_API_PORT=3101
# QM_POSTGRES_PORT=5433
# QM_REDIS_PORT=6381
# QM_MINIO_API_PORT=9010
# QM_MINIO_CONSOLE_PORT=9011
